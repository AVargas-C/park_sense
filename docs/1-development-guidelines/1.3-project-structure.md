# 3. Project Structure
> **Project:** ParkSense — Full-Stack IoT Parking Occupancy System
> **Date:** 2026-01-24
> **Author:** Arturo Vargas Cuevas
> **↑ Parent:** [[1-development-guidelines]]
---

## Repository Layout

```
parksense/
├── firmware/
│   ├── CMakeLists.txt              # Top-level CMake build
│   ├── cmake/
│   │   ├── arm-none-eabi.cmake     # Cross-compilation toolchain file
│   │   └── version.cmake           # Version injection script
│   │
│   ├── libs/                       # Vendor SDKs (git submodules — do not modify)
│   │   └── vendor_sdk/             # git submodule: vendor HAL, BSP, and peripheral libraries
│   │       ├── Drivers/
│   │       │   ├── mcu_hal_driver/         # Layer 1 HAL — use as-is
│   │       │   ├── BSP/<board-name>/       # Sensor/peripheral driver reference from vendor BSP
│   │       │   └── CMSIS/                  # Cortex-M core headers + startup files
│   │       └── Middlewares/
│   │           ├── network_library/        # Network driver (WiFi/Ethernet abstraction)
│   │
│   ├── targets/                    # Per-binary entry points (three build targets)
│   │   ├── node/
│   │   │   ├── CMakeLists.txt
│   │   │   └── main_node.c         # Layer 5 — Node super loop
│   │   ├── gateway/
│   │   │   ├── CMakeLists.txt
│   │   │   └── main_gw.c           # Layer 5 — Gateway super loop
│   │   └── bootloader/
│   │       ├── CMakeLists.txt
│   │       └── btl_main.c          # Layer 2 — Bootloader entry
│   │
│   ├── src/                        # Shared modules (compiled into applicable targets)
│   │   │
│   │   ├── bsp/                    # Layer 1 — Project BSP (wraps vendor HAL)
│   │   │   ├── bsp.c               # Clock tree, peripheral ownership, low-power entry
│   │   │   ├── bsp.h
│   │   │   └── mcu_target/         # Board-specific files for the target MCU
│   │   │       ├── startup_<mcu>.s
│   │   │       ├── node.ld         # Linker script — Node binary
│   │   │       ├── gateway.ld      # Linker script — Gateway binary
│   │   │       └── bootloader.ld   # Linker script — Bootloader binary
│   │   │
│   │   ├── drivers/
│   │   │   ├── sensor/             # Layer 3a — Sensor Drivers (Node only)
│   │   │   │   ├── sensor_api.h        # Common sensor interface
│   │   │   │   ├── tof/                # ToF sensor driver — adapted from vendor BSP
│   │   │   │   │   ├── tof_driver.c
│   │   │   │   │   └── tof_driver.h
│   │   │   │   └── magnetometer/       # Magnetometer driver — adapted from vendor BSP
│   │   │   │       ├── mag_driver.c
│   │   │   │       └── mag_driver.h
│   │   │   │
│   │   │   ├── rf/                 # Layer 3b — RF Driver
│   │   │   │   ├── rf_api.h            # Common RF interface (Node + Gateway)
│   │   │   │   └── rf_module/          # RF module driver — SED (Node) / Coordinator (GW)
│   │   │   │       ├── rf_driver.c
│   │   │   │       └── rf_driver.h
│   │   │   │
│   │   │   └── net/                # Layer 3c — Network Driver (Gateway only)
│   │   │       ├── net_api.h           # Abstract transport interface (WiFi or Ethernet)
│   │   │       ├── wifi/
│   │   │       │   ├── wifi_driver.c   # WiFi module driver via network library
│   │   │       │   └── wifi_driver.h
│   │   │       └── ethernet/
│   │   │           ├── eth_driver.c    # WIZnet W5500 driver via ioLibrary (Phase 2)
│   │   │           └── eth_driver.h
│   │   │
│   │   ├── cpm/                    # Layer 4b — Communication Protocol Module
│   │   │   ├── cpm.c               # Packet assembly, ACK/retry, encryption (Node + GW)
│   │   │   └── cpm.h
│   │   │
│   │   ├── pdm/                    # Layer 4a — Parking Detection Module
│   │   │   ├── pdm.c               # Occupancy FSM, sensor fusion (Node only)
│   │   │   └── pdm.h
│   │   │
│   │   ├── bootloader/             # Layer 2 — Bootloader logic
│   │   │   ├── btl_validate.c      # ECDSA P-256 signature + CRC-32 verification
│   │   │   ├── btl_jump.c          # Jump-to-application
│   │   │   └── btl.h
│   │   │
│   │   └── ota/                    # OTA update module
│   │       ├── ota.c
│   │       └── ota.h
│   │
│   ├── include/                    # Shared public headers
│   │   ├── common_types.h
│   │   ├── error_codes.h
│   │   └── version.h               # Auto-generated by build
│   │
│   └── test/                       # Unit tests (host-based)
│       ├── CMakeLists.txt
│       ├── test_pdm.c
│       ├── test_cpm.c
│       ├── mocks/
│       │   ├── mock_sensor_api.c
│       │   ├── mock_rf_api.c
│       │   └── mock_net_api.c
│       └── unity/                  # Unity test framework (git submodule)
│
├── tools/
│   ├── inject_header.py            # Inject ps_image_header_t + ECDSA P-256 signature
│   └── version_gen.py              # Generate version.h from git tags
│
├── server/                         # Python Flask backend (doc 5.16)
├── gui/                            # React dashboard (doc 5.18)
├── docs/                           # All design documentation
├── .github/
│   └── workflows/
│       └── ci.yml                  # GitHub Actions pipeline
│
├── .gitmodules                     # Submodule entries (vendor_sdk, unity)
├── .gitignore
├── LICENSE
└── README.md
```
